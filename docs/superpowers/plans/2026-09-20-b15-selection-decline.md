# B15 — доля отказов отбора: метод, инструмент, и место под результат

> **Статус: ЗАМЕР ПРОВЕДЁН 2026-09-21.** Корпус написал владелец, не глядя на таблицу триггеров;
> число и разбивка — в §7. **Ответ `B15`: доля неоднозначности 0 %, то есть многоходовой протокол
> уточнения на этом корпусе — украшение, а не продукт.** Но корпус вскрыл находку крупнее той,
> которую замер искал (§7.2, §7.3), и она адресована A4′.
>
> **Составлен:** 2026-09-20, дерево на `cf25d24`; **§7 заполнен** 2026-09-21. Ветка `launcher--7`,
> Этап 6 (A4′), Трек 1. Не спека и не ADR. Обязательство, которое он исполняет, несёт строка `B15`
> в [`sidr-agentic-master-plan-v1.0.md`](../../governing/sidr-agentic-master-plan-v1.0.md) §3.6.
>
> **Инструкция по регенерации входов и запуску** — не здесь, а в
> `data/repository/src/test/resources/b15/README.md`, рядом с самими входами. Здесь — **зачем** и
> **как читать результат**.

---

## 1. Какое решение это питает, и почему без числа его принимать нельзя

У A4′ три ненаписанные спеки (Master Plan §3.3). Вторая — **протокол уточнения**: многоходовое
«агент спросил → человек ответил → план уточнён → продолжили». Сегодня его нет ни в одном
контракте: `PlanResult.Clarify(question)` — терминальный лист.

Построить его дорого: новый контракт в домене, новое состояние поверхности, персистентность
висящего вопроса, стык с гейтом согласия. Вопрос — стоит ли, и он не решается вкусом.

A1″ спека §6.3 приняла осознанное решение: `ToolSelector` при неоднозначности **отказывает, а не
угадывает** — «ранжирование, разрывающее ничью, выбрало бы за пользователя эффект молча; отказ
стоит промаха, а промах бесплатен». Это было верно при четырёх инструментах. Сейчас их **243**, и
`docs/improvements.md` (запись 4) возражает по существу: при такой массе промах, упирающийся в
стену, читается как «оно не работает».

Кто прав — зависит от частоты, и Master Plan формулирует порог прямо:

> **5% отказов — уточнение украшение. 40% — уточнение и есть продукт.**

## 2. Почему разбивка, а не одно число

Отказы ведут к **разным** решениям, и суммарная доля их смешивает.

| Категория | Что произошло | Что из неё следует |
|---|---|---|
| `PLANNED` | инструмент совпал, план построен, FastPath не перехватил | работает |
| `SHADOWED_BY_FASTPATH` | инструмент совпал, но FastPath решил раньше — шаг 2b не достигнут | триггер недостижим; класс A1′ (шесть из тринадцати) |
| `HANDLED_BY_FASTPATH` | FastPath решил сам | не отказ вообще |
| `DECLINE_AMBIGUOUS_AUTHORED` | два авторских триггера заявили текст | **уточнение конвертирует** |
| `DECLINE_DYNAMIC_TIE` | два ярлыка подошли одинаково | **уточнение конвертирует** |
| `DECLINE_DYNAMIC_UNQUALIFIED` | ярлык назван, приложение — нет | **уточнение конвертирует**; плюс вопрос стоп-слов |
| `DECLINE_NO_MATCH` | текст не заявил никто | уточнение **не помогает** — нечего предлагать. Дыра словаря |
| `DECLINE_UNRESOLVED_TARGET` | инструмент совпал, имя не разрешилось | **другая проблема: `R14-39`**, отсюда сырой текст уходит в облако |
| `DECLINE_MISSING_ARG` | обязательный аргумент пуст | словарь и дескриптор разошлись в имени аргумента |

**Решает доля НЕОДНОЗНАЧНОСТИ, а не доля отказов.** Сто процентов `NO_MATCH` — аргумент против
протокола уточнения, а не за него: он говорит «нужны инструменты и триггеры», а не «нужен диалог».

## 3. Решения владельца, 2026-09-20

| Развилка | Решение |
|---|---|
| Объём A4′ | **фазами**, с фазовыми границами (прецедент A1″), приёмка одна, в конце |
| `B15` | **прогнать до спеки A4′** — решение о многоходовом уточнении не принимать вслепую |
| `B16` (локальный вывод) | **записать адрес, в Этапе 6 не трогать** |
| Две строки доктрины | **обе внести сейчас** — исполнено, `7b66413` |
| Источник реестра для замера | **снять каталог с телефона один раз, дальше мерять офлайн**; фикстура git-ignored, коммитится только отчёт |

Последняя развилка решалась потому, что меняет **смысл** числа, а не его точность: 205 из 225
инструментов были динамическими уже в A1″, и неоднозначность живёт именно там. Замер над одними
авторскими инструментами систематически смещён в известную сторону — формулировка, метившая в
ярлык, попадает в `NO_MATCH` вместо своей настоящей категории.

## 4. Что снято с устройства, 2026-09-20

| Что | Сколько | Чем |
|---|---|---|
| Динамические имена инструментов | **223** из **66** пакетов | `ShortcutToolSource.names()` — продакшен-проекция, не `LauncherApps` напрямую |
| Установленные приложения | **144** | `InstalledAppsRepositoryImpl` — продакшен-репозиторий |

Устройство: SM-A325F, Android 13, Sidr держит `android.app.role.HOME`. Приложение под тестом
**не тронуто** — `lastUpdateTime` совпал до и после; ставился только пакет проб
`com.sidr.launcher.test`. `connectedAndroidTest` не использовался (он сносит приложение вместе с
принятой схемой-4 базой).

**Каталог дрейфует, и это причина существования фикстуры.** Смоук-раунд A1″ намерил **216** ярлыков
2026-09-16; этот прогон намерил **223** 2026-09-20. Приложения обновляются и публикуют другие
ярлыки. Число, перемеренное против движущегося реестра, несравнимо с предыдущим — поэтому реестр
замораживается, а не опрашивается заново на каждый прогон.

**Побочное наблюдение, полезное при чтении результата:** в снятом каталоге **ноль** пар
«приложение + ярлык», совпадающих дословно. Естественных ничьих ровно такой формы на этом телефоне
нет, поэтому `DECLINE_DYNAMIC_TIE` может выйти нулём по свойству каталога, а не по свойству
селектора.

## 5. Что инструмент делает, и чего он НЕ делает

Классификатор — `SelectionDeclineMeasurement` (`:data:repository`, unit). Прогоняет корпус через
**настоящий** путь: реальный `RuleBasedIntentMatcher`, реальный `ToolSelector` над реестром из
**243** инструментов, реальный `ToolMatchPlanner` с разрешением по 144 приложениям.

**Вердикт всегда из продакшен-кода; здесь выводится только подпричина.** `ToolSelector.select`
возвращает `null` по четырём поводам и не говорит по какому. Ничья считается опросом
**продакшен**-матчера по одному имени за раз. Единственное место, где одна строка правила
пересказана, — `looksLikeUnqualifiedShortcut`, и она метит уже принятый отказ, а не выносит вердикт.

### Три названных ограничения — до замера, а не после

1. **Самопроверка зажгла пять категорий из девяти.** Не зажглись `SHADOWED_BY_FASTPATH`,
   `DECLINE_AMBIGUOUS_AUTHORED`, `DECLINE_DYNAMIC_TIE`, `DECLINE_MISSING_ARG`. Это предел
   синтетического корпуса из семи строк, **не** доказательство недостижимости — но и обратного не
   показано, поэтому **нуль в этих категориях неотличим от мёртвой ветки**.
2. **Фраза, перехваченная FastPath, считается обработанной, даже если под ней пряталась
   неоднозначность.** Классификатор проверяет вердикт FastPath **раньше**, чем подпричину отказа.
   Это честно по отношению к вопросу B15 («что пользователь переживает как отказ»), но латентную
   неоднозначность скрывает. Наблюдено живьём в самопроверке: `настройки` ушло в
   `HANDLED_BY_FASTPATH`, а не в `open_system_settings` — тот самый эффект затенения, за который
   заплатил A1′.
3. **`isUndecided` скопирован дословно** из `RouteCommandUseCase.kt:188` (`Unknown || LowConfidence`),
   потому что он приватный. Если предикат изменится, копия станет неверной и **ничто об этом не
   скажет**. Единственный осознанный drift-риск инструмента, записанный, а не скрытый.

## 6. Что требуется от владельца

Файл `data/repository/src/test/resources/b15/corpus.txt` — **20–30 строк, одна формулировка в
строке**. Уже под git-ignore.

**Правило, которое делает замер замером:** корпус пишется **не глядя на таблицу триггеров**.
Корпус, написанный автором триггеров, измеряет автора — и агент поэтому написать его не может по
определению, а не по скромности.

- Пишется то, что владелец **реально сказал бы** телефону, а не то, что знает реализованным.
- **Не редактируется после написания.** Падежи, лишние слова, разговорные формы — предмет замера.
- Формулировки, про которые есть подозрение, что не сработают, — **самые ценные**: корпус из одних
  попаданий даёт 0% отказов и ноль информации.
- `ru` / `tr` / `en` вперемешку. `ToolVocabulary.matchIn` уплощает локали, поэтому матчинг
  локале-слепой и все три формы живы из любой локали.
- Строки с `#` игнорируются.

**Граница, при смещении которой замер даёт неверный ВЫВОД, а не просто шум:** строка корпуса — это
то, что набирают в командную строку, ожидая действия. Пожелания к продукту («хочу чтобы понимал
контекст») формально прогонятся, но все упадут в `NO_MATCH`, и отчёт скажет «неоднозначности нет,
протокол не нужен» — вывод, сделанный из того, что корпус измерял не то. Пожеланиям место в
[`docs/improvements.md`](../../improvements.md), это другой артефакт с другим потребителем.

Многошаговые формулировки («открой календарь и поставь встречу») **в корпусе уместны** — они честно
измерят, как часто задача формулируется составной, а это вход в ту же спеку A4′.

## 7. Результат — прогон 2026-09-21

**Корпус: 25 формулировок**, написан владельцем, не глядя на таблицу триггеров, не редактирован
после написания. `ru` / `tr` / `en` вперемешку. **Реестр: 243 инструмента** (223 ярлыка + 20
авторских), фикстура от 2026-09-20. Прогон — `SelectionDeclineMeasurement`, `--rerun-tasks`,
`build/test-results` очищен.

| Категория | Штук | Доля |
|---|---:|---:|
| `PLANNED` | **0** | 0,0 % |
| `SHADOWED_BY_FASTPATH` | 0 | 0,0 % |
| `HANDLED_BY_FASTPATH` | 13 | 52,0 % |
| `DECLINE_AMBIGUOUS_AUTHORED` | 0 | 0,0 % |
| `DECLINE_DYNAMIC_TIE` | 0 | 0,0 % |
| `DECLINE_DYNAMIC_UNQUALIFIED` | 3 | 12,0 % |
| `DECLINE_NO_MATCH` | 9 | 36,0 % |
| `DECLINE_UNRESOLVED_TARGET` | 0 | 0,0 % |
| `DECLINE_MISSING_ARG` | 0 | 0,0 % |
| **Отказов всего** | **12** | **48,0 %** |

### 7.1. Ответ на вопрос, ради которого замер делался

**Доля неоднозначности — НОЛЬ, а не 12 %, и это исправление сделано по измерению, а не по
впечатлению.** Отчёт показал три `DECLINE_DYNAMIC_UNQUALIFIED`; проверка того, **какой именно**
ярлык совпал, разоблачила все три:

| Формулировка | Что на самом деле совпало |
|---|---|
| `Send a message "Hello" to Azat on Telegramm` | ярлык **«Send»** приложения **Cake Wallet** |
| `Что произощло сегодня в Палестине` | ярлык **«Сегодня»** приложения **Obsidian** |
| `приведи в порядок мой телефон, очисти кэш, …` | ярлык **«Приложения»** приложения **Galaxy Store** |

Ни одно не является неоднозначностью, которую человек захотел бы разрешить: это **случайные
совпадения обиходных слов** с названиями чужих ярлыков. Селектор отказал **правильно**. Ошиблась
диагностическая метка — ровно **ограничение (1) §5**, названное до замера, и здесь оно сработало
в сторону завышения.

> **Вердикт `B15`: при пороге Master Plan «5 % — украшение, 40 % — продукт» доля неоднозначности
> в этом корпусе равна 0 %. Многоходовой протокол уточнения не конвертировал бы НИ ОДНОЙ строки.
> Для A4′ это ответ «украшение», и он получен измерением, а не спором.**

Оговорка, обязательная к цитированию вместе с вердиктом: корпус — **25 строк одного человека на
одном телефоне**. Это не доказательство, что неоднозначности не бывает; это доказательство, что на
реальном обиходе владельца она **не является узким местом**, и что тратить A4′ на неё нельзя, пока
узкое место другое.

### 7.2. Находка, которой замер не искал, и она крупнее той, что искал

**`PLANNED` = 0. Ни одна из 25 реальных формулировок не дошла до зарегистрированного инструмента.**
Вся масса A1″ — 20 авторских инструментов и 223 ярлыка — не была достигнута **ни разу**.

Разбивка `HANDLED_BY_FASTPATH` (13 строк) показывает, **почему**, и ни одна её половина не является
успехом:

**Семь строк — `Message(NoAppFound)`.** Все семь вида «открой X …», где FastPath извлёк имя
приложения, включив в него остаток фразы: `gmail и проверь новые письма`, `protonmail and check for
messges from Anthropic`, `календарь и поставь урок на завтра в восемь`, `the terminal and write a
script`, `новости в Телеграмм`, `онлайн-банк`, `online-banking app and transfer money to mom`.
**Проверено по дереву, а не предположено:** `RouteCommandUseCase:120-121` ловит ровно
`CommandMessage.NoAppFound` и запускает `GoalShape.AppNotInstalled(message.query)` — план
«запустить → предложить магазин». **То есть в продукте эти семь строк становятся предложением
поискать в Play Store выдуманное имя приложения.**

**Шесть строк — `Executed`, все `SEARCH[WEB]`**, и запрос — дословный сброс остатка фразы вместе
со служебными словами и вторым предложением:

| Формулировка | Что реально загуглено |
|---|---|
| найди дешевые билеты в Бишкек и сделай бронь | `дешевые билеты в бишкек и сделай бронь` |
| найди в интернете лекцию по Rust | `в интернете лекцию по rust` |
| найди в интернете книгу по языку Лисп и скачай ее | `в интернете книгу по языку лисп и скачай ее` |
| найди мне работу | `мне работу` |
| найди классный фильм на youtube | `классный фильм на youtube` |
| найди лучший рецепт шашлыка | `лучший рецепт шашлыка` |

**Одна строка из двадцати пяти сделала то, о чём просили** — последняя. Остальные пять несут в
поисковом запросе слова, которые запросом не являются.

### 7.3. Доктринальное следствие — самое ценное, что произвёл этот замер

ADR 1/4 и `DOC-ADL-3` гарантируют, что **промах** FastPath доходит до планировщика: «промах
FastPath — больше не основание ответить „Неизвестная команда"». Они **ничего не говорят о неверном
решении** FastPath.

Измерено: **13 строк из 25 были «решены» FastPath, и по прочтению отчёта 12 из 13 решены неверно.**
Решённость — это и есть то, что закрыло им оба пути дальше:

- шаг **2b** не достигается, потому что он требует `isUndecided()`, — **реестр из 243 инструментов
  не консультируется**;
- шаги **5–7** не достигаются по той же причине — **модель не консультируется**.

> **FastPath объявлен «оптимизацией задержки, а не фильтром на понимание». На корпусе владельца он
> работает фильтром на понимании в 52 % случаев** — не потому, что промахивается, а потому, что
> отвечает уверенно и неверно. Формулировка доктрины покрывает промах и не покрывает ошибку.

Это адресовано **A4′** и, возможно, требует **третьей поправки `DOC-ADL-3`** по change-control
(прецедент: две уже были, 2026-08-20 и 2026-08-22). Решение — владельческое, и оно **не** принято
этим документом.

### 7.4. Что вышло нулями и почему это нуль, а не мёртвая ветка

Обязательный пункт: нуль в категории неотличим от неработающей ветки, если не сказать, какое из
двух.

| Категория | Нуль означает |
|---|---|
| `DECLINE_AMBIGUOUS_AUTHORED` | **не доказано.** Ветка не зажглась ни в самопроверке, ни здесь — ограничение (1) §5 в силе |
| `DECLINE_DYNAMIC_TIE` | **объяснено свойством каталога:** в снятой фикстуре ноль дословно совпадающих пар «приложение + ярлык» (§4) |
| `DECLINE_UNRESOLVED_TARGET` | **ветка живая** — зажглась в самопроверке (`удали приложение несуществующееприложение`); нуль здесь значит, что владелец не писал команд удаления |
| `DECLINE_MISSING_ARG` | **не доказано** — не зажигалась ни разу |
| `SHADOWED_BY_FASTPATH` | **не доказано.** Требует совпадения инструмента И решения FastPath; при `PLANNED = 0` шансов не было |
| `PLANNED` | **ветка живая** — зажглась в самопроверке (`youtube подписки`, `wifi settings`); нуль здесь — результат, а не дефект инструмента |

### 7.5. Чего этот замер НЕ измерил

- **«Совпал не тот инструмент».** Судит только владелец. Отчёт печатает `fastpath:` и `acted:`
  на каждую строку именно для того, чтобы это суждение стало возможным; вывод §7.2 о «одной строке
  из двадцати пяти» — **прочтение отчёта**, а не измерение, и владелец вправе его пересмотреть.
- **Латентную неоднозначность** под строками, перехваченными FastPath — ограничение (2) §5.
- **Другие корпуса.** 25 строк одного человека.

## 8. Что результат решит, и чего не решит

**Решит:** строить ли многоходовой протокол уточнения в A4′ (спека 2 из трёх, §3.3), по доле
**неоднозначности**.

**Не решит и решать не должен:**
- **достаточность словаря.** `DECLINE_NO_MATCH` — вход в другое решение (триггеры и инструменты),
  и оно не A4′.
- **`R14-39`.** `DECLINE_UNRESOLVED_TARGET` — доктринальный долг с собственным адресом, и его доля
  здесь лишь показывает масштаб, а не выбирает починку.
- **«совпал не тот инструмент».** Ни один гвард и ни один классификатор этого не видит: строка,
  ушедшая в `PLANNED`, могла спланировать **не то**. Судит только владелец, глазами, по колонке
  соответствия в отчёте.
