# SIDR — переход от умного роутера к агентной системе

> **STATUS: APPROVED (владелец, 2026-08-18).** Стратегический маршрут агентного трека, не план блока.
> Снимает развилки, блокировавшие A1–A6 с 2026-07-11. Этапы 1/0/2/3 исполнимы по этому документу;
> Этапы 4–7 заходят обычным циклом `brainstorm → spec → plan → build`. См. §«Как использовать этот документ».

## Context

**Зачем этот план.** Sidr прошёл Stage 1 (AI-Launcher MVP) и весь дизайн-трек DS-1…DS-11 + I18N-1.
Инженерная база сильная: `:domain` чист (stdlib+coroutines, проверяется каждый блок), приватность
выражена исполняемыми guard-тестами, каждый блок закрывается ADR + приёмкой на устройстве.

Но заявленная цель — агентная ОС — не начата. Собственный ADR от 2026-07-10 постановил «next
architectural slice = A1»; спеки A1–A6 написаны 2026-07-11. За 5,5 недель после этого закрыто шесть
дизайн-блоков и **ноль агентных**; все шесть планов A1–A6 до сих пор `STATUS: PROPOSED`.

Причина структурная: у дизайн-трека есть Master Plan (2087 строк), милстоуны, DoD, change-control —
он всегда «готов к следующему блоку». У агентного трека — шесть спек и четыре нерешённые развилки, он
всегда «требует решения». Трек с бо́льшим процессом выигрывает независимо от важности. Этот план
устраняет эту асимметрию и снимает четыре развилки.

**Что конкретно мешает движку сегодня:**

1. **У агента нет рук.** `DefaultActionCatalog` — 7 действий, 5 из них «открыть что-то у себя».
   Многошаговый план поверх этого даёт «открой приложение → открой приложение».
2. **Понимание заперто за префиксным матчером.** `RuleBasedIntentMatcher` = 7 глаголов
   (`open/launch/start`, `install`, `search/find/google`) + 2 ключа настроек + 7 строк команд.
   Правило «LLM консультируется только при low-confidence» означает, что модель никогда не видит
   задачу целиком. Многошаговые цели структурно не проходят этот фильтр.
3. **Матчер понимает только английский.** Все константы — английские литералы. На устройстве
   владельца (системная локаль `ru-RU`, интерфейс русский) «открой телеграм» → `UnknownIntent`
   confidence 0.10; `llmRouterEnabled` по умолчанию `false` ⇒ **русская команда офлайн не работает
   вообще.** `LocaleCompletenessGuardTest` это не ловит: он проверяет `strings.xml`, а константы
   матчера — hardcoded Kotlin-множества в `:data:repository`.
4. **~1400 строк инертного ONNX** едут в каждом APK. OQ#1/#2 открыты 14 месяцев. Классификатор на
   7 меток не извлекает аргументы (это делает `SlotExtractor` регуляркой) и по построению
   ограничен до подсказки (`NluConfidenceCalibrator` мапит в `[0.50, 0.85)`).
5. **Платформа ушла вперёд.** `AppFunctions`, `MCP`, `Gemini Nano`, `AICore`, `ADK` — ноль
   упоминаний во всей документации.
6. **Цель «ПК» отсутствует в доках**, а решение по портируемости ядра дешевеет только сегодня.

**Ожидаемый результат.** Работающий агентный движок: цель → план → гейт → инструмент → наблюдение →
результат → трассировка, с руками из реестра ОС, портируемым ядром и сохранением всех инвариантов
приватности и согласия.

---

## Решения владельца (зафиксированы в этой сессии)

| # | Развилка | Решение |
|---|---|---|
| 1 | Что такое Stage 2 «AI Framework» | **Портируемое ядро агента** с двумя потребителями: Android-оболочка и ПК. Stage 2 как отдельная стадия расписания упраздняется — слои строятся внутри вертикальных срезов |
| 2 | Локальный ИИ | **Кэш выученных планов** вместо модели в процессе. ONNX-классификатор удаляется, OQ#1/#2 закрываются. AICore / локальный режим — позже, как ускорители |
| 3 | Язык команд | Локализация глаголов **сразу, в Этапе 0** |
| 4 | deterministic-first | **Переопределить**: модель владеет пониманием, детерминизм — исполнением. Ограничение «7 глаголов» снимается — это была временная мера для демонстрации MVP |

---

## Новое правило вместо «rule-first before any LLM call»

Заменяет формулировку в `CLAUDE.md` (Hard rules) и `docs/architecture.md` (Principles):

```
Понимание принадлежит модели. Исполнение принадлежит детерминированному слою.

1. FastPath (детерминированный, локализованный) отвечает на частотные точные команды
   без модели. Это оптимизация задержки, НЕ фильтр понимания.
2. Кэш выученных планов воспроизводит уже понятые формы целей детерминированно и офлайн.
3. Всё остальное идёт к планировщику-модели. Промах FastPath больше НЕ является
   основанием ответить «Unknown command».
4. Ничто из предложенного моделью не исполняется, не получает прав и не покидает
   устройство иначе как через детерминированные ворота:
   ToolRegistry → валидация аргументов → preconditions → риск-гейт/consent →
   границы цикла → egress allow-list → трассировка.
5. Router-off / offline / no-key ⇒ FastPath + кэш планов + честное «нужна сеть».
   Байт-в-байт паритет rule-only остаётся проверяемым тестом.
```

**Следствие, требующее решения внутри Этапа 1:** `FeatureFlags.llmRouterEnabled` (сейчас `false`)
перестаёт быть воротами понимания. Рекомендация — понимание доступно, когда сконфигурирован
провайдер; явный переключатель «только локально» сохраняется для пользователей, которым он нужен.

## Правило роста: функционал масштабируется, границы — нет

Замысел владельца — наращивать функционал по мере роста потребностей, не фиксируя облик системы
заранее. Это принимается и распространяется на поверхность (UI появляется вслед за способностью) и
на абстракции (обобщать при появлении второго потребителя, а не в предвкушении — существующий
принцип feature-first).

Из этого правила **исключаются границы**. Реестр как единственный путь к миру, валидация аргументов,
гейт согласия, лимиты цикла, трассировка, egress allow-list и откат закладываются на **первом же
срезе, для двух инструментов** — не «когда понадобится». Причина механическая: если рантайм сначала
научился вызывать 20 инструментов, а согласие добавляется потом, это 20 мест, где его можно забыть.
Если согласие живёт внутри `ToolExecutor` с первого дня, инструмент №21 получает его бесплатно.
Та же логика, по которой guard-тесты приватности в этом проекте написаны до того, как появилось что
утекать.

---

## Как использовать этот документ

Это **не** план блока в смысле репозитория (`docs/superpowers/plans/*.md` = пронумерованные задачи с
файлами и тестами, по которым идёт сессия). Он стоит **выше спек**: снимает развилки, блокировавшие
агентный трек с 2026-07-11, и задаёт очередь блоков.

```
CLAUDE.md → decisions.md (ADR) → specs/*.md → plans/*.md (Task 1..N) → сессия → гейт + ADR
                    ▲
            этот документ решает, ЧТО и в каком порядке сюда попадает
```

| Этап | Исполним по этому документу? |
|---|---|
| 1 (стратегические ADR), 0 (расчистка), 2 (тулчейн + KMP), 3 (агентный Master Plan) | **Да** — файлы и содержание определены |
| 4–7 (A0, A1′, A4′, A2/A3, A5/A6) | **Нет** — каждому нужна своя спека и свой план с задачами, обычным циклом `brainstorm → spec → plan → build` |

Кикофф сессии (по образцу `ai-launcher-mvp-plan.md` §0):

```
Работаем над Этапом <N> из docs/superpowers/plans/<этот-файл>.md.
Прочитай CLAUDE.md (Hard rules) и §«Решения владельца» + раздел нужного этапа.
Этап закрывается зелёными testDebugUnitTest + assembleDebug, записью ADR
и синхронизацией CLAUDE.md / current-status.md.
```

**Порядок: Этап 1 идёт первым.** Стратегические решения фиксируются до кода — прецедент AIL-4, где
блокирующий ADR писался перед реализацией. В частности 0.3 (удаление 1400 строк ONNX) исполняет
решение ADR №2; без него это было бы оправдание задним числом.

---

## §0. Протокол сессии

Кикофф — **шаблон, а не новый текст на каждый шаг**. Меняется только номер этапа.
(Тот же приём, что `ai-context/ai-launcher-mvp-plan.md` §0.)

### Кикофф (вставить в начало новой сессии)

```
Работаем над Этапом <N> из docs/superpowers/plans/2026-08-18-agentic-track-restart.md.

Прочитай сначала:
  - этот план: §«Решения владельца», §«Новое правило вместо rule-first»,
    §«Правило роста», раздел Этапа <N>, и §HANDOFF в конце (если непустой)
  - CLAUDE.md — Hard rules и Current goal

Соблюдай forks-before-code: неразрешённый форк — спросить владельца ДО кода,
не додумывать. Объём — строго раздел этапа, без расширения.

Этап закрывается только по «Чеклисту закрытия» из §0 плана.
```

### Чеклист закрытия (обязателен для каждого этапа)

- [ ] сделано ровно по разделу этапа, объём не расширен
- [ ] `./gradlew --no-daemon testDebugUnitTest assembleDebug` зелёные под JDK 17;
      вывод **не** пропущен через `tail` (это уже маскировало красный гейт как exit 0)
- [ ] ADR добавлен в `ai-context/decisions.md` в принятом формате
- [ ] `CLAUDE.md` и `ai-context/current-status.md` синхронизированы
- [ ] раздел этапа в этом плане помечен ✅ с абзацем результата
- [ ] **§HANDOFF в конце плана переписан для следующего этапа**
- [ ] **агент ПРЕДЛАГАЕТ коммит** (сообщение + скоуп) — владелец говорит да/нет; агент коммитит,
      но **никогда не пушит**. Сессия не заканчивается работой, оставленной только в рабочем дереве:
      либо коммит, либо явное «это черновик, выбрасываю»

### Почему коммит, а не «оставлю в дереве»

Гранулярность — **не сессия, а наименьшее самосогласованное состояние**. В I18N-1 это дало ~28
коммитов за 8 дней при 16 задачах; иногда 4–5 за сессию, иногда ноль (аналитической сессии коммитить
нечего).

Коммит в агентной работе покупает три вещи:

1. **Восстановление.** 2026-08-18 в этом репозитории: незакоммиченный черновик плана был перезаписан
   и потерян навсегда; `LocaleCompletenessGuardTest` (+96 строк) в том же дереве уцелел, потому что
   был закоммичен (`bce726c`). Одинаковый риск, разный исход — разница только в коммите.
2. **Поверхность ревью.** `.superpowers/sdd/` состоит из `review-<sha>..<sha>.diff` — ревьюер-агент
   смотрит диff между коммитами. Нет коммитов — нет ревью.
3. **Передача контекста.** `git log --oneline -5` — самый дешёвый handoff. Поэтому качество
   сообщений (conventional commits + скоуп блока) не церемония, а рабочий канал.

**Не коммитить:** при красном гейте (чинить или парковать на scratch-ветке) и при выбрасываемой
разведке.

**Экспозиция:** ветка `launcher--7` на 2026-08-19 была на 38+ коммитов впереди `codeberg/launcher--7`
и не запушена. Пуш — решение владельца, но пока его нет, локальный git остаётся единственной копией.

### Модель на этап

| Этап | Модель | Почему |
|---|---|---|
| 1 — четыре ADR | **Opus, high** | правит инварианты; два открытых форка |
| 0.2, 0.3, 0.5, 0.6, 0.7 | Sonnet | ошибку ловит тест или компилятор |
| 0.4 — сжать CLAUDE.md | **Opus** | нужно отличить несущее от архива |
| 2 — тулчейн, KMP | Sonnet | ломается громко |
| 3 — агентный Master Plan + доктринальная матрица | **Opus, high** | проектирование процесса; разбор доктрины |
| 4–7 — спеки и срезы | **Opus, high** | архитектура агента |

Правило: **Opus там, где решение записывается в документ и живёт годами; Sonnet там,
где ошибку ловит компилятор или тест.**

### Subagent-driven (SDD)

Применять на Этапах 4–7, когда у блока появится план с пронумерованными независимыми
задачами — как I18N-1 (16 задач) и DS-6B (12). Не применять на Этапах 0–3: задачи там
связанные, а два форка Этапа 1 требуют владельца, которого субагент спросить не может.

---

## Этап 0 — Расчистка (после ADR №1 и №2 из Этапа 1)

Дни, не недели. Каждый пункт — отдельный коммит.

Зависимости: 0.2 требует ADR №1 (переопределение deterministic-first — FastPath), 0.3 требует
ADR №2 (платформенная переоценка), 0.6 требует ADR №2 (бюджеты). 0.1 / 0.4 / 0.5 / 0.7 независимы
и могут идти параллельно с Этапом 1.

### 0.1. Разблокировать релиз
`checkOwnerReviewedLocaleStrings` держит `:app:assembleRelease` / `assemble` / `bundleRelease` /
корневой `build` красными. Релизного артефакта сейчас не существует.
**Действие:** владелец проставляет `OWNER-REVIEWED` в 10 файлах `strings_locked.xml`.
**Проверка:** `./gradlew :app:assembleRelease` → exit 0.

### 0.2. Локализовать FastPath (закрывает баг на устройстве владельца)
**Файл:** `data/repository/src/main/java/com/sidr/launcher/data/repository/intent/RuleBasedIntentMatcher.kt:179-193`

Множества `LAUNCH_VERBS` / `INSTALL_VERBS` / `SEARCH_VERBS` / `SETTINGS_KEYWORDS` /
`SIMPLE_COMMANDS` расширяются формами `ru` и `tr`. Матчер остаётся Android-free — словарь остаётся
Kotlin-константами (ресурсы недоступны в `:data:repository` и не нужны: это не UI-текст).

- `CommandNormalizer.normalize` уже лужит `Locale.ROOT` — турецкая «I»/«ı» обработана корректно,
  менять не нужно.
- Русские формы нуждаются в морфологии минимально: покрыть повелительное наклонение и инфинитив
  («открой/открыть/запусти/запустить/найди/найти/установи/установить»).
- Существующий escape-hatch `ResolveCommandWithAliasUseCase` (S2-2) остаётся ручным дополнением.

**Новый guard:** `FastPathLocaleGuardTest` в `:data:repository` — по образцу
`app/src/test/java/com/sidr/launcher/i18n/LocaleCompletenessGuardTest.kt`: каждое множество обязано
иметь непустой набор форм для каждой поддерживаемой локали, иначе тест красный. Это не даёт забыть
язык при добавлении нового.

**Проверка:** таблично-управляемый тест в `RuleBasedIntentMatcherTest` — «открой телеграм»,
«telegramı aç», «найди погоду» дают те же интенты с confidence 0.90, что и английские аналоги.

### 0.3. Удалить ONNX-стек
Радиус проверен. Удаляются:

- модуль `:data:ai-local` целиком (18 файлов) + `include` в `settings.gradle.kts` +
  `implementation(project(":data:ai-local"))` и ONNX AAR в `app/build.gradle.kts`
- `:app` DI: `di/NluMatcherProvidesModule.kt`, `di/NluMatcher.kt`,
  `di/ModelProvisionProvidesModule.kt`, `di/ModelProvisionBindsModule.kt`
- `:app` работа: `work/ModelDownloadWorker.kt`, `work/WorkManagerModelDownloadScheduler.kt`,
  `test/SessionLifecycleReleaseTest.kt`
- `SidrLauncherApp.kt` — `ModelManager.ensureModel()` из `onCreate`, `SessionLifecycle`-набор,
  ONNX-ветка `onTrimMemory`/`onLowMemory` (WorkManager и `Configuration.Provider` остаются —
  ими пользуется `SuggestionPrecomputeWorker`/`UsageCleanupWorker`)
- `:data:repository` — `intent/LayeredIntentMatcher.kt`, `intent/NluConfidenceCalibrator.kt`;
  незаквалифицированный `IntentMatcher` связывается напрямую с `RuleBasedIntentMatcher`
- `:data:repository` — `ModelAvailabilityRepositoryImpl` + ключ `model_available_ids`
  (обновить `ALL_KEY_NAMES` и `PrivacyInventoryGuardTest`)
- `:data:ai-cloud` — `KtorModelDownloader.kt`
- `:domain` — `ai/local/` (`ModelId`, `ModelAvailability`, `ModelAvailabilityRepository`,
  `TextEmbedder`, `ModelDownloader`, `ModelFilePresence`), `MatcherSource.NLU`

**Сохраняется:** `DeviceProfile` / `DeviceCapability` / `DeviceProfileProvider` /
`AndroidDeviceProfiler` — их использует гейтирование предпосчёта подсказок. `LocalInferenceGate` —
решение при исполнении: это естественные ворота для будущего AICore-уровня, но сейчас он завязан
только на ONNX; если не переиспользуется в Этапе 0 — удалить, вернуть при появлении локального
уровня.

`SuggestionsProvidesModule.kt` импортирует `ModelDownloadConfig` (для `TextEmbedder`, Block V) —
эту зависимость снять.

**Проверка:** `./gradlew testDebugUnitTest assembleDebug` зелёные; APK меньше на размер ONNX AAR
(зафиксировать до/после в ADR); `:domain` classpath по-прежнему stdlib+coroutines.

### 0.4. Сжать `CLAUDE.md`
90 KB → ~10–15 KB: текущее состояние, hard rules, указатели. История переезжает в `decisions.md`,
где ей место. Сейчас каждая сессия платит налог на чтение архива закрытых фаз с июня.

### 0.5. Честные статусы
Ввести `CODE-GREEN` / `DEVICE-ACCEPTED` / `CLOSED`. Пересмотреть текущие «CLOSED» и вернуть в
видимость device-долг: DS-5 (device-pending с 2026-07-13), I18N-1 (offline / TalkBack / fontScale 2.0
/ системный language picker непроверены), DS-6B (MWL-времена сверены только между реализациями).

### 0.6. Переписать бюджеты производительности на измеренные

`docs/architecture.md` объявляет бюджеты «hard targets». Фактически:

| Бюджет | Статус |
|---|---|
| Cold start `< 400 ms` | **не достигнут ни разу.** Траектория: 2021 ms (debug) → **504 ms** (release pre-R8, лучшее за историю) → 527 (R8) → 750 (Baseline Profile) → **766 ms** (финальный release). В `CLAUDE.md` уже понижен до «aspirational, not a ship gate» |
| Heap 80 / 150 / 250 МБ | **ни разу не измерялся.** «~98MB → ~75MB» в Y1/Y2 — размер APK после R8, не heap |
| Инфраструктура | `baselineprofile/` содержит только `BaselineProfileGenerator.kt`; ни `StartupTimingMetric`, ни `MemoryUsageMetric`. Замеры делались вручную через `am start -W` |

Проблема не в том, что числа не выполняются, а в том, что **они уже плавают без гейта**: 504 → 766
сложилось из блоков с хорошими локальными причинами, и ни одно отдельное решение не виновато.
Недостижимое число перестаёт быть ограничением.

**Заменить один тип чисел на три:**

1. **Инвариант (не плавает)** — свойства, а не миллисекунды: первый кадр без спиннера (достигнуто),
   мгновенное ощущение от FastPath, выживание процесса при уходе в фон. Нарушение = сломанный продукт.
2. **Измеренная база + гейт регрессии (плавает только осознанно)** — холодный старт и heap.
   Вместо `< 400 ms` записать `766 ms сегодня; блок, ухудшающий это, требует записанного решения`.
3. **По профилю устройства (действительно плавающее)** — уже есть: «any feature that cannot meet
   them must degrade or be disabled on that device profile». Механизм сохранить.

**Действия:** замерить heap на устройстве (`dumpsys meminfo`) — впервые; зафиксировать реальные
значения как базу; при желании закрепить — `MacrobenchmarkRule` + `StartupTimingMetric` в
существующий модуль `baselineprofile/`.

**Не меняет вывода по локальной LLM.** 250 → 400 → 500 МБ ничего не решает: 1B-модель в int4 —
700 МБ–1 ГБ резидентно, это другой порядок. Настоящее ограничение не в числе, а в том, что лаунчер
обязан мгновенно воскрешаться: самый жирный процесс убивается LMK первым. Дверь открывает не
смягчение бюджета, а **AICore** (модель в системном процессе, heap приложения не тратится).

### 0.7. ✅ ЗАКРЫТО (2026-08-19) — оба пункта сняты, один был ложным

- `PrayerSummaryMapper` `contentDescription` — **исправлено** коммитом `85b5f0a`
  («fix(i18n-2): Home prayer names go through the string seam, not the raw enum»):
  имена намазов идут через `sidrString`, строки в `strings_locked.xml` для en/ru/tr,
  покрыто `LauncherScreenPrayerStripTest` + `PrayerSummaryMapperTest`.
- `AndroidPrayerLocationProvider.DEVICE_LOCATION_LABEL = "Current location"` — **не баг.**
  Это **идентичность, а не отображаемый текст**: `GetPrayerContextUseCase.matchesSetup`
  сравнивает по ней `PrayerScheduleProvenance.locationLabel` при проверке валидности кэша,
  поэтому перевод константы инвалидировал бы кэш у каждого пользователя с device-локацией.
  Видимая строка локализуется отдельно, при рендере, из `PrayerLocationSource.DEVICE`
  (`homeLocationLabelText` в `:feature:launcher`, `locationLabelText` в `:feature:prayer`).
  Задокументировано в KDoc константы.

**Следствие для Этапа 1:** запись в `CLAUDE.md` о «hardcoded Current location label» как об
известном пробеле I18N-2 устарела и должна быть снята при синхронизации документов.

---

## Этап 1 — ✅ ЗАКРЫТ (2026-08-19) — Пакет ADR

**Результат.** Четыре ADR приняты и записаны в `ai-context/decisions.md`
(«ADR 1/4 … 4/4 (agentic restart)»), оба открытых форка сняты владельцем в начале сессии, до текста:
(1) `llmRouterEnabled` **инвертируется на НОВЫЙ ключ** `flag_local_only` (`FeatureFlags.localOnlyMode`,
default `false`) — простой флип дефолта был бы инертен на любой установке, где хоть раз сохраняли
настройку (`PreferencesMapper` пишет весь объект; точный прецедент DS-11 `autoHideNavBar`);
(2) ADR 4 — **один контур, две поверхности**: единый `AgentSession`/`Planner`, план из 0 шагов *есть*
реплика (Assistant), план из N шагов — задача (Tasks/Agents). Обязательное ограничение по `ActionIds`
зафиксировано в ADR 3 с обеими причинами (`launch_app` = Room PK в `resolution_preferences`; все семь =
исходящий wire-контракт к LLM). Развилка A1 сознательно **не** решена — она принадлежит Этапу 5.

Правки документов в том же коммите: `CLAUDE.md` (Hard rules — новое правило вместо
«matching ≠ generation»/rule-first; Current goal), `docs/architecture.md` (Principles, Target stack,
**бюджеты переписаны в трёхуровневую схему**, AI execution pipeline получил целевую форму),
`docs/roadmap.md` (Stage 2 упразднён как стадия расписания; guiding principle заменён; OQ#1/#2/#3
закрыты; sequencing summary переписан под этапы 0–7), `docs/agentic-os-architecture.md` (шапка-поправка,
§2.2, §4.2, §5 build order → вертикальные срезы), `docs/adr/ADR-001-hybrid-ai.md` (§2 void, §4 →
LiteRT/LiteRT-LM, статус «superseded in part»), `ai-context/current-status.md`. Кода не тронуто:
`git diff --stat` по этому коммиту содержит только `.md`. Гейт — ниже, в чеклисте закрытия.

**Найдено попутно, требует внимания Этапа 0:** пункт HANDOFF о снятии устаревшей записи про
«hardcoded Current location label» уже был выполнен коммитом `8ca58ab` — в `CLAUDE.md:81` оба пункта
записаны как закрытые I18N-2. Действий не потребовалось.

### Исходное задание этапа (для истории)

Четыре записи в `ai-context/decisions.md` + правки `CLAUDE.md` / `docs/architecture.md` /
`docs/roadmap.md` / `docs/agentic-os-architecture.md`.

1. **Deterministic-first переопределён** — текст правила выше. Правит Hard rules и
   `architecture.md` Principles. Включает решение по `llmRouterEnabled`.
2. **Platform re-baseline 2026** — `AppFunctions` / `MCP` как источники инструментов; ONNX NLU
   закрыт; OQ#1/#2 закрыты, OQ#3 закрыт вместе с `TextEmbedder`.

   **Назначенный рантайм локального вывода — LiteRT / LiteRT-LM.** Не ONNX, не raw llama.cpp;
   MediaPipe LLM Inference построен поверх LiteRT, то есть прежняя формулировка «MediaPipe/llama.cpp»
   называла обёртку вместо основы. Три причины:
   - единый доступ к NPU всех крупных чипсетов через `CompiledModel API` (LiteRT 2.x), JIT-подход —
     одна модель, акселератор выбирается в рантайме;
   - **function calling с constrained decoding + `FunctionGemma` + Tool Use APIs** — агентный
     workload из коробки, а не классификация;
   - Android + iOS + **Windows + Linux** — один рантайм на обе цели портируемого ядра (ADR 3).

   **Записанная альтернатива — ExecuTorch, с явным условием переключения.** Отклонён сейчас по трём
   причинам: AOT-компиляция под конкретный бэкенд даёт матрицу `.pte` на произвольном парке устройств
   (`minSdk 28`, сайдлоад, любой чипсет), тогда как LiteRT решает это JIT; function calling /
   constrained decoding в него не входят — это рантайм, слой приносишь сам; и его главное
   преимущество (свой PyTorch-пайплайн) в проекте отсутствует — PyTorch здесь ноль.
   **Условие переключения:** появление собственного дообучения планировщик-модели на своих данных
   (кэш выученных планов — естественный источник пар «цель → план»). Тогда PyTorch → fine-tune →
   ExecuTorch органичнее, чем конвертация под LiteRT.

   **Выбор остаётся заменяемым, пока порт `Planner` рантайм-агностичен** (спека A4 это уже
   обеспечивает). ADR фиксирует направление, а не выжигает его в архитектуру.

   `AICore` / Gemini Nano — отдельный путь для устройств, где он есть: модель живёт в системном
   процессе, heap приложения не тратится вовсе; ограничение — железо.

   Фиксирует причину, по которой локальность достигается **кэшем планов, а не моделью в процессе**:
   не конкретное число бюджета, а порядок величины и роль процесса. 1B-модель в int4 — 700 МБ–1 ГБ
   резидентно; лаунчер обязан мгновенно воскрешаться, а самый тяжёлый процесс LMK убивает первым.
   **Уточнение:** для малой function-calling модели (`FunctionGemma`, малые Gemma 4) на NPU порядок
   иной — 200–500 МБ, вес может быть memory-mapped. В сочетании с 0.6 это переводит локальное
   планирование из «невозможно» в **«реально на способном железе, как opt-in»**. Прежняя
   формулировка «смягчение бюджетов ничего не меняет» была верна для 1B и слишком широка как общее
   утверждение.

   Фиксирует также переход бюджетов из «hard targets» в трёхуровневую схему из 0.6 — как записанную
   правку `docs/architecture.md`, а не молчаливое ослабление. И правит `docs/adr/ADR-001-hybrid-ai.md`
   п.4 («future MediaPipe LLM or llama.cpp integration» — устарело).
3. **Portable core boundary** — что именно составляет «Framework»: `domain/agent`, `domain/tool`,
   `domain/context`, `domain/memory`, `domain/trace` + существующий `:domain`. Решение по KMP
   (см. Этап 2). Android и ПК — два потребителя, а не две кодовые базы.
4. **Assistant ⊕ Agent** — один диалоговый контур или осознанно два. Сейчас
   `GenerateReplyUseCase` (говорит) и `CommandPlanner` (маршрутизирует) разделены hard rule'ом;
   для агента нужен контур, где агент и говорит, и действует, и уточняет многоходово.

---

### Обязательное ограничение, которое ADR 2 или 3 должен зафиксировать

Найдено при разборе развилки A1 (2026-08-19) и проверено по коду. Оно верно при любом варианте A1′,
при федерации и при KMP, поэтому записывается **сейчас**, до того как кто-то напишет A1′.

```
ActionIds — семь значений, и у них ДВА РАЗНЫХ контракта, а не один:

  launch_app        → Room PRIMARY KEY. `resolution_preferences`
                      (primaryKeys = ["action_id","query","context_key"], Migration1To2).
                      Единственная production-точка записи —
                      ResolveCommandWithPreferenceUseCase.kt:34 (CapabilityKey(LAUNCH_APP, slot)).
                      Смена значения оставляет осиротевшие строки, занимающие retention-квоту
                      (200 записей), а не «просто сбрасывает предпочтения».

  все семь          → исходящий wire-контракт: CatalogSchemaRenderer рендерит их в промпт LLM,
                      ProposalValidator валидирует по ним ответ модели.
                      Под RouterOutboundGuardTest и DefaultActionCatalogRouterSchemaGuardTest.

Требование к A1′: семь текущих значений сохраняются БАЙТ-В-БАЙТ.
`ActionIds.kt:12` уже это фиксирует («MUST stay stable — persisted/wire contract»);
ADR обязан повторить и указать ПОЧЕМУ, потому что причина у launch_app и у остальных шести разная.
```

**Что НЕ фиксировать:** саму развилку «параллельный словарь против эволюции на месте». Она
принадлежит Этапу 5 и была разобрана против спеки от 2026-07-11, до ADR 2. Её главный пробел —
что идентификаторы придут извне (AppFunctions: `<pkg>/<fn>`, MCP: `<server>/<tool>`) — это ровно то,
что вводит ADR 2; после него обе опции развилки построены на неверном допущении «словарь закрыт».
Решать в Этапе 5, на переписанной спеке.

---

## Этап 2 — Тулчейн + портируемость

**2.1. Обновление зависимостей.** AGP 8.7.3 / Kotlin 2.0.21 / Compose BOM 2024.12 / compileSdk 35 —
конец 2024 года. `known-risks.md` сам называет dependency drift риском. `AppFunctions` требует
свежего тулчейна. Блокер, а не гигиена.

**2.2. `:domain` → KMP.** Сейчас `domain/build.gradle.kts` = `kotlin.jvm` + `jvmToolchain(17)` и
единственная зависимость `coroutines.core`. Перевод на `kotlin.multiplatform` с таргетами
`android`/`jvm` — почти механический **именно сегодня**. Каждый следующий агентный блок пишется
как чистый домен и увеличивает цену этого решения.

**Проверка:** `./gradlew build` зелёный; `:domain` compileClasspath по-прежнему stdlib+coroutines;
все 333 доменных теста проходят без изменений.

---

## Этап 3 — Агентный Master Plan + извлечение доктринальной матрицы

Две обязанности, делаются одним заходом: они друг друга определяют, и извлекать доктрину раньше
этого момента нельзя (см. 3.2).

### 3.1. Агентный Master Plan

Документ того же качества, что дизайновый: последовательность блоков, Definition of Done,
change-control, милстоуны, критерии приёмки. **Без него агентный трек снова проиграет дизайновому
по той же причине, что и в июле.**

Включает определение готовности:

```
Agentic Shell v1 = DONE, когда на реальном устройстве:
1. ≥1 реальная цель исполняется планом из ≥3 шагов с наблюдением и перепланом при сбое
2. ≥15 настоящих инструментов из ≥2 уровней (свои + AppFunctions/системные интенты)
3. Каждый переход риска и смена уровня останавливают цикл; отмена и правка шага работают
4. Трассировка 1:1 с реальностью; частичный результат показан как частичный
5. Обратимые шаги откатываются; необратимые помечены ДО исполнения
6. Шаблонные цели работают без сети; отсутствие облака деградирует честно
7. Egress агента под allow-list, доказано guard-тестом
8. Всё, что лаунчер умеет сегодня, работает как сегодня
9. Device-accepted владельцем
```

### 3.2. Извлечение доктринальной матрицы

**Проблема.** Восемь исламских принципов (Niyyah · Amanah · Mizan · Ilm · Adl · Haya · Sukun ·
Human authority) живут внутри `docs/design/SIDR Design System Master Plan.md` — документа
**дизайн-трека**. Но половина строк матрицы §5 указывает на инженерные слои: Niyyah → A4,
Ilm → A4–A5, Adl → A1/A4/A6, Human authority → A4/A6. Агент, работающий над рантаймом, не имеет
причин открывать «Design System Master Plan». Категориальная ошибка, которая станет дорогой ровно
тогда, когда появится второй трек.

**Почему именно на Этапе 3, а не раньше.** До агентного Master Plan у доктрины **один** потребитель,
и извлекать общее незачем. После — два, и документ перестаёт принадлежать одному из треков. Та же
логика, что «Framework = ядро с двумя потребителями» и что правило A2/A3 «строить по потребителю».

**Что извлекать — операционную часть, не философию:**

| Остаётся в Master Plan | Переезжает |
|---|---|
| §4 — восемь принципов прозой (философия) | §5 Traceability Matrix |
| | §5.1 приоритет при конфликте |
| | §5.2 verification vocabulary |
| | §20.1 calm budgets |

Это разделение принципиально, а не косметическое. Аудит §21 записал **Риск 5 — «исламская эстетика
станет декоративной»**. Отдельный документ с принципами прозой читается как манифест и тянет в
декоративную сторону; отдельный документ-таблица с типами проверок и именами тестов читается как
инженерное ограничение. Один материал, разный жанр, разная судьба.

**Форма — четыре колонки вместо нынешних пяти прозаических:**

```
ID          правило                               тип проверки   чем обеспечено
DOC-AML-1   egress только через allow-list        arch-guard     AiRequestGuardTest
DOC-ADL-1   одинаковый risk → одинаковый gate     unit           <нет> ← долг A1′
DOC-SKN-1   idle animation budget = 0             screenshot     <goldens>
DOC-ILM-2   EXTERNAL-инструмент несёт provenance  arch-guard     <нет> ← долг A1′
DOC-NYH-1   шаг с неоднозначным аргументом
            уточняет, а не догадывается           unit           <нет> ← долг A4′
```

Три вещи, которых сейчас нет и которые всё меняют:

1. **Стабильный ID** — ADR и тест ссылаются на правило, а не пересказывают его. Пересказ есть
   механизм расхождения документов (прецедент: три конфликта v1↔v1.1, разбиравшиеся в DS-0).
2. **Тип из закрытого словаря §5.2, проставленный в самой таблице.** Сейчас словарь определён
   (`screenshot`/`semantics`/`arch-guard`/`unit`/`device`/`manual`), но к матрице не применён —
   в колонке проза.
3. **Колонка «чем обеспечено»** — имя файла теста либо честное `<нет>`. Превращает документ из
   декларации в **список долгов**: пустая ячейка видна сразу.

**Что делает его системным, а не девятым документом:** guard-the-guard, уже изобретённый в этом
репозитории (`module_prefixes_cover_every_module_that_ships_strings`,
`scoped_roots_cover_every_ui_module`). Тест читает эту таблицу и краснеет, если правило заявляет
`arch-guard`, а такого теста в репозитории нет. Документ перестаёт стареть незаметно.

**Попутно чинится филинг** (дёшево сделать заодно):
- Master Plan лежит в `docs/design/`, объявленной ADR «DS-0» архивом (*«archive of inputs, not the
  governing spec»*), но в его список семи документов не входит и по `CLAUDE.md` является
  управляющим. Живой документ среди архивных.
- Шапка Master Plan: «Текущая точка: DS-0 ✅ · DS-1 ✅ · следующий блок DS-2» — отстала на
  ~10 блоков (закрыты DS-1…DS-11 + I18N-1).
- Имя файла без версии (`SIDR Design System Master Plan.md`), при том что сам документ рекомендует
  `sidr-design-system-master-plan-v1.2.md`. Появление v1.3 пройдёт незаметно.

---

## Этап 4 — A0 Thin Agentic Spike

**Первый вертикальный срез. Цель — доказать движок, а не построить слои.**

Одна реальная двухшаговая цель проходит `goal → plan → gate → tool → observe → tool → result → trace`
и принимается на устройстве. Минимальные куски A1/A2/A4; **без** A3/A5/A6, без DAG, без переплана.

Новое в `:domain` (портируемое ядро):
- `domain/tool/` — `ToolId`, `ToolDescriptor`, `ToolInvocation`, `ToolResult`, `ToolRegistry`,
  `ToolExecutor` (порты). Растёт из `ActionCatalog` / `ActionDescriptor` / `LauncherAction`
- `domain/agent/` — `AgentGoal`, `ExecutionPlan`, `PlanStep`, `Planner` (порт), `AgentExecutor`,
  `AgentSession`, `ExecutionState`, `RuntimeBudget`, `ConsentCheckpoint`
- `domain/trace/` — `ExecutionTrace`, события шагов

Переиспользуется без изменений:
- `ProposalValidator` (`domain/ai/router/`) — образец fail-closed валидации аргументов
- `ExecuteActionUseCase` → `IntentActionResolver` → `ActionExecutor` — становится
  `SYSTEM_INTENT`-уровнем `ToolExecutor`
- `ActionRiskLevel` + `SidrActionGate` (`core/ui`) — гейт согласия
- `OutboundContextPolicy` — граница выхода агента
- `OperationResult` — fail-closed везде

UI: минимальная поверхность выполнения на существующих примитивах DS-5
(`SidrActionProposal`, `SidrResultSurface`, `SidrErrorSurface`, `SidrProvenanceLine`,
`SidrActionGate`). Ничего нового в `core/ui` не создавать.

**Поверхность — функция состояния, а не макета.** Один компонент на состояние рантайма; композиция
собирается из того, что сессия реально сообщила. Тогда план на 2 шага и план на 7 шагов, появление
`Blocked` или переход в `AwaitingConsent` не требуют редизайна — облик масштабируется сам.
Нарисованный Execution Stream (аудит §12.4: линейный список из трёх шагов) — **вход, а не спека**:
он не выражает переменное число шагов, замену шага при переплане, согласие как прерывание шага,
неравные длительности, DAG-план и девять состояний сессии. Прецедент — DS-10, где устаревшая
премисса собственной спеки уступила реальности.

**Обязательное подготовительное действие:** расщепить `LauncherViewModel`
(`feature/launcher/.../LauncherViewModel.kt`, 886 строк, 13 зависимостей, 9 зон ответственности).
Агентный рантайм станет десятой. Аудит предупреждал об этом как о Риске 1 — расщеплять **до** A4.

---

## Этап 5 — A1′ Федеративный ToolRegistry

Переписывает `docs/superpowers/specs/2026-07-11-a1-tool-capability-design.md`: реестр — не каталог
своих семи действий, а **федерация адаптеров над одним словарём**.

```
                    ToolRegistry  (портируемое ядро)
                              │
      ┌───────────┬───────────┼────────────┬──────────────┐
 InAppTools   SystemIntent  AppFunctions  MCP        Accessibility
 (свои 7)     (Android)     (Android 15+) (ПК+сеть)  (Tier 2, opt-in)
```

`AppFunctions` и `MCP` — одна форма (самоописывающая функция со схемой, обнаруживаемая в реестре).
Один контракт, N адаптеров — это то, что делает ПК ещё одним адаптером, а не переписыванием.

**`DoctrineGuardTest`** — этика в CI, по образцу существующих guard-тестов
(`AiRequestGuardTest`, `RouterOutboundGuardTest`, `PrayerLocationPrivacyGuardTest`):
каждый инструмент с эффектом `EXTERNAL` обязан нести provenance; ни один шаг не исполняется без
записи в trace; одинаковый risk → одинаковый гейт; egress только через allow-list.

---

## Этап 6 — A4′ Рантайм полностью

### Модель исполнения: лаунчер — процесс, агент — сессия

Не разобрано ни в одной из шести спек, при этом определяет форму продукта. Специфичная для Android
ловушка: **собственное действие агента выбрасывает пользователя из процесса, где агент живёт.**

```
Шаг 1: открыть Календарь → пользователь в Календаре → лаунчер в фоне → процесс может быть убит
Шаг 2: ...                 кто исполняет?
```

Для обычного лаунчера это норма — он своё отработал. Для агента это ломает цикл в **типичном**, а не
краевом случае.

**Ответ — четыре слоя:**

1. **Корректность (обязательна, от процесса не зависит).** `AgentSession` сохраняется после каждого
   перехода состояния. Смерть процесса = прерывание того же класса, что ожидание согласия → `Paused`.
   При возврате пользователя сессия восстанавливается и предлагает продолжить. Возобновление
   идемпотентно. Механика уже требуется спекой A4 («persisted, inspectable state machine»,
   «duplicate resume/confirm cannot execute a step twice») — теперь ясно, зачем.
2. **Удобство (пока процесс жив).** Цикл крутится в существующем `@ApplicationScope`
   (инжектится в `SidrLauncherApp`). Переживает уничтожение Activity, поворот, уход в фон без
   убийства. Обычный случай, без церемоний.
3. **Непрерывность (только когда шаг обязан продолжаться, пока человек в другом приложении).**
   Foreground service с обязательным уведомлением — единственный честный механизм в Android, и он
   совпадает с доктриной: видно, что работает, можно остановить. `android-limitations.md` разрешает
   ровно этот случай («justified and user-visible»).
4. **Никогда.** Фоновые автономные циклы без видимости — уже в non-goals спеки A4, оставить.

**Отдельный процесс агенту не нужен.** Рантайм — машина состояний и несколько объектов, это байты;
единственное тяжёлое в системе (модель) по построению вне процесса (облако либо AICore).
`android:process=":agent"` добавил бы IPC, второй граф Hilt и вторую точку отказа ради экономии байтов.

### Три спеки, которых нет ни в одном документе

1. **Откат и компенсация.** A4 упоминает `PartiallyCompleted` как состояние — и всё. «Шаг 3 из 5
   упал, шаги 1–2 уже изменили мир» не заспецифицировано нигде. Дизайн-док про это говорит
   («Aftermath / undo surface»), домен молчит.
2. **Протокол уточнения.** `PlanResult.Clarify(question)` — терминальный лист. Многоходового
   «агент спросил → человек ответил → план уточнён → продолжили» нет ни в одном контракте.
3. **Кэш выученных планов** — механизм локальности из решения #2. Обобщает
   `ResolutionPreferenceStore` (S2-1) и `AliasStore` (S2-2) с одного действия до плана: цель понята
   моделью один раз → сохраняется шаблон плана (не формулировка) → воспроизводится детерминированно,
   офлайн, на любом железе. Многоязычно бесплатно: шаблон привязан к плану, формулировки
   накапливаются вокруг.

---

## Этап 7 — A2 / A3 по потребителю, затем A5 / A6

**Спеки A2 и A3 остаются целиком — они написаны верно.** Потребитель у них назван и существует по
замыслу: это `Planner` из спеки A4 (`plan(goal, ContextSnapshot, ToolRegistry, UserMemory)`).
`agentic-os-architecture.md` прямо определяет себя как «direction, not a committed plan» — описывать
целевую форму целиком и есть работа архитектурной спеки, иначе A1 примет решения, блокирующие A2.

Риск не в том, что форма описана, а в том, что спека потом читается как **список к постройке**.
В репозитории есть оба исхода:

| Прецедент | Что сделали | Результат |
|---|---|---|
| **AIL-1** | «ships only the contract + the family vocabulary; the concrete catalog + impl is deferred to AIL-2» | Потребитель пришёл через день, всё сошлось |
| **`:data:ai-local`** | Построено полностью (`ModelStore`, `ModelProvisioner`, `Sha256Verifier`, `KtorModelDownloader`, `ModelDownloadWorker`, `Scheduler`) против `ModelDownloadConfig.INTENT_NLU_PENDING` | Потребитель не пришёл за 14 месяцев; 1400 строк и AAR в каждом APK — удаляются в 0.3 |

Разница не в качестве спеки, а в том, что решил план.

**Правило для A2/A3:** словарь и контракты — целиком (паттерн AIL-1); реализуется только то, что
читает первый планировщик A4; непостроенное помечается в спеке как «спроектировано, не построено,
триггер постройки — <какой>».

### Обязательное проектное решение A2: двойная проекция контекста

Принимается **при проектировании A2, не после** — задним числом это переделка всего слоя контекста,
сейчас стоит почти ничего.

```
ContextEngine
  ├── projectForCloud(snapshot) → узкая, редуцированная, под OutboundContextPolicy
  └── projectForLocal(snapshot) → широкая: сырые данные, НИКОГДА не покидающие устройство
```

**Почему это не преждевременная абстракция, а следствие уже принятых решений.**
`OutboundContextPolicy.ALLOWED` содержит ровно четыре значения — `USER_COMMAND`,
`STATIC_SYSTEM_PROMPT`, `GENERATION_LIMITS`, `ACTION_CATALOG_SCHEMA`. Это значит, что **облачный
планировщик планирует вслепую**: он не видит календарь, местоположение, список приложений и память.
То есть приватностная архитектура проекта — работающая и правильная — **структурно ограничивает
потолок полезности облачного планировщика**, потому что именно эти данные делают агента личным
(«найди свободный вечер», «успею ли до магриба», «открой то, чем я обычно пользуюсь сейчас»).

Спека A2 эту напряжённость чувствует и предлагает компромисс — расширить allow-list на
*редуцированные* поля снимка. Локальной модели компромисс не нужен: ей можно отдать сырой календарь
целиком, потому что ничего не уходит с устройства. **Это и есть настоящая причина, по которой
серьёзной агентной системе нужен локальный вывод — не офлайн и не цена, а доступ к личному
контексту.**

Следствия:
- проекции две с первого дня, даже пока локального планировщика нет — тогда он подключается, а не
  требует переделки;
- `projectForLocal` по построению никогда не проходит через egress-слой; guard-тест обязан это
  доказывать (кандидат в `DOC-AML-*` доктринальной матрицы);
- порт `Planner` (`TemplatePlanner → LocalPlanner → LlmPlanner` в спеке A4) уже резервирует место —
  задача A2 в том, чтобы контекст был готов его наполнить.

Локальный планировщик записывается как **назначенный уровень с зарезервированным швом**, а не как
«ускоритель, может быть, когда-нибудь». Второе при реализации игнорируют.

**Почему здесь цена лишнего поля выше обычной.** Каждое поле `ContextSnapshot` обязано пройти
классификацию в `OutboundContextPolicy`, получить guard-тест против утечки сырых данных, быть
permission-gated и fault-isolated отдельно, и пережить приватностное ревью в каждом последующем
блоке. Неиспользуемое поле здесь — не мёртвый код, а постоянное обязательство в самом чувствительном
слое. То же с A3: `MemoryConflict`/`MemoryMutation` подразумевают политику разрешения конфликтов,
которую до появления конфликтующего писателя пришлось бы писать против воображаемых случаев.

Переиспользовать: существующие `SuggestionProvider`-ы уже fault-isolated и permission-gated —
готовые провайдеры контекста; `ResolutionPreferenceStore` (S2-1) и `AliasStore` (S2-2) — два
реальных хранилища памяти для унификации.

A5/A6 — только после того, как A4 выдаёт реальные трассы.

### PREVIEW-поверхности — остаются (решение владельца, 2026-08-18)

Пять `PREVIEW`-вкладок (Tasks / Agents / Activity / Terminal / Interaction Moments) **не удаляются**.
Бейдж `PREVIEW` — это в точности мера, предписанная аудитом (§21, Риск 4: «roadmap должен пометить
их `AVAILABLE NOW` / `FOUNDATION` / `FUTURE CONTRACT`»), поэтому golden rule не нарушен. Слоты в
таб-баре резервируют место и заранее учат пользователя форме — когда A4 приедет, вкладка не
появляется внезапно.

Снимается не сама поверхность, а налог на сопровождение — сейчас решение о превью принимается заново
в каждом сквозном блоке (DS-11 — проверка cmap ради маркера `•`; I18N-1 — отдельное owner-решение об
исключении из локализации; дальше по очереди a11y, темы, матрица goldens, fontScale):

1. **Политика, записанная один раз:** PREVIEW-поверхности по умолчанию исключены из сквозной работы
   (i18n, a11y, goldens, темы). Вопрос перестаёт задаваться в каждом блоке.
2. **Срок годности у каждой вкладки:** `tasks` / `agents` → реальны в A4, `activity` → в A5,
   `terminal` / `interaction moments` → назвать блок или пометить бессрочным демо. Иначе превью
   незаметно становится постоянной мебелью.
3. **Устранить расхождение в доках:** DS-0 фиксирует «target IA = 4 поверхности, 5-табовая навигация
   отложена до A4», а шипнутый `SidrTabBar` уже имеет пять вкладок (`home/apps/tasks/agents/activity`).
   Обновить DS-0 под фактическую IA либо явно записать три слота как резерв.
4. **Оговорка на A4:** когда движок приедет, поверхность определяет он, а не нарисованный макет.

---

## Verification

**Каждый этап:**
```bash
./gradlew --no-daemon testDebugUnitTest assembleDebug          # JDK 17
./gradlew --no-daemon :core:ui:verifyRoborazziDebug            # goldens
```
Не пропускать вывод через `tail` — это уже маскировало красный гейт как exit 0 (2026-07-13).

**Этап 0 дополнительно:**
- `./gradlew :app:assembleRelease` → exit 0 (после 0.1)
- `RuleBasedIntentMatcherTest`: «открой телеграм» / «telegramı aç» / «найди погоду» → интент с
  confidence 0.90, паритет с английскими формами
- `FastPathLocaleGuardTest` красный при отсутствии формы для любой поддерживаемой локали
- после 0.3: `:domain` compileClasspath = stdlib + coroutines; размер APK до/после в ADR
- на устройстве (`ru-RU`, `llmRouterEnabled=false`, авиарежим): «открой телеграм» запускает
  Telegram — регрессия, ради которой делается 0.2

**Этап 2:** все 333 доменных теста проходят после перевода на KMP без изменения исходников тестов.

**Этап 4 (приёмка A0):** на SM-A325F одна реальная двухшаговая цель проходит цикл целиком;
гейт согласия останавливает выполнение на переходе риска; отмена в середине не выполняет
следующий шаг; трассировка содержит каждый шаг; `git diff` по существующим ViewModel-сьютам пуст
(паритет). Отдельно проверяется переживание процесса: `adb shell am force-stop` в середине плана →
повторный запуск показывает сессию в `Paused` с честным предложением продолжить; двойное
подтверждение возобновления не исполняет шаг дважды.

**Постоянные инварианты, проверяемые в каждом блоке:**
- `:domain` без Android / Compose / Ktor / Hilt / Room / DataStore / `core/*`
- отсутствие `feature → feature` рёбер
- операции возвращают `OperationResult`, не бросают в UI
- `OutboundContextPolicy` allow-list не расширен молча — guard-тест
- router-off / offline / no-key ⇒ паритет FastPath (тест сохраняется)
- ключ BYOK не в логах: `adb logcat | grep -c 'sk-'` → 0

---

## Что сознательно НЕ делается

- Не строится **работающая** логика Agents / Automation / Activity до реального движка (golden rule).
  Существующие `PREVIEW`-макеты остаются как есть — см. Этап 7
- Не превращается ни одна `PREVIEW`-вкладка в живую поверхность раньше своего блока (A4 / A5)
- **Не откладываются границы.** Согласие, лимиты, трассировка, откат и egress-allow-list не
  добавляются «потом, когда понадобится» — они есть с первого среза
- Не подгоняется рантайм под нарисованный макет (фиксированное число шагов, скрытые состояния,
  анимация вместо честного ожидания)
- Не переписывается существующий rule/router/executor путь — A1–A6 компонуются над ним
- Не публикуется SDK для сторонних разработчиков — «портируемое ядро» означает двух своих
  потребителей (Android, ПК), а не публичный API
- Не добавляется автономность без согласия и не обходятся риск/permission гейты

---

## HANDOFF

> Переписывается **закрывающейся сессией** для следующей. Читается кикоффом.

**Состояние на 2026-08-19:** Этап 1 ✅ закрыт (четыре ADR + правки шести документов, кода ноль).
Ни один другой этап не начат.

**Следующий этап:** 0 — расчистка. Модель: Sonnet на 0.2 / 0.3 / 0.5 / 0.7, **Opus на 0.4** (сжатие
`CLAUDE.md` — надо отличить несущее от архива), 0.6 требует замера на устройстве.

**Что нужно знать входящей сессии:**

- **Оба форка Этапа 1 сняты владельцем, переспрашивать не нужно.** Решения записаны в ADR 1/4 и 4/4:
  `llmRouterEnabled` → **новый ключ** `flag_local_only` (`FeatureFlags.localOnlyMode`, default `false`),
  старый ключ осиротеет и выпадает из `ALL_KEY_NAMES`, миграции нет; Assistant и Agent — **один контур,
  две поверхности**.
- **Порядок внутри Этапа 0 не свободен.** ADR 1/4 зафиксировал: **0.2 (локализация FastPath) —
  предусловие для смены флага**, а не независимая уборка. С инвертированным флагом промах FastPath на
  `ru`/`tr` перестаёт быть «Unknown command», но стоит сетевого раунд-трипа за то, что должно быть
  мгновенным, а офлайн честно деградирует. Сначала словарь, потом флаг.
- **0.3 — радиус удаления перечислен пофайлово в разделе Этапа 0**, добавлений к списку не найдено.
  Два решения принимать при исполнении: судьба `LocalInferenceGate` (ADR 2/4 разрешает удалить и
  вернуть позже, если он не переиспользуется) и снятие импорта `ModelDownloadConfig` из
  `SuggestionsProvidesModule.kt`. Зафиксировать размер APK до/после — ADR 2/4 на это ссылается.
- **0.6 требует первого в истории проекта замера heap** (`dumpsys meminfo` на устройстве).
  `docs/architecture.md` уже переписан в трёхуровневую схему, и в тир 2 стоит явное
  «heap — not yet measured, first measurement is Этап 0.6». Замер закрывает эту дырку; до него таблица
  честна, но неполна.
- **0.1** (`OWNER-REVIEWED` в 10 файлах `strings_locked.xml`) — действие владельца, агент его не
  выполняет; идёт параллельно.
- Развилку A1 «параллельный словарь против эволюции на месте» по-прежнему **не решать** — Этап 5,
  на спеке, переписанной после ADR 2/4.
- При правках `:domain` помнить про Этап 2.2: перевод на KMP уже решён (ADR 3/4), поэтому не стоит
  заводить в домене ничего JVM-специфичного.
